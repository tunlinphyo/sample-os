import '../style.css';
// import { SystemController } from './system.controller';
import './system.css';

document.body.dataset.schema = parent.device.theme;

document.addEventListener('DOMContentLoaded', () => {
    // new SystemController(parent.device, parent.clock);
});